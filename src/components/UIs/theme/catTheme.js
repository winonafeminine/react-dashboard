import createTheme from '@mui/material/styles/createTheme';


export const catTheme = createTheme({
    typography: {
        fontFamily: [
            '"Prompt"', 
            'sans-serif'
        ].join(',')
    }
});
