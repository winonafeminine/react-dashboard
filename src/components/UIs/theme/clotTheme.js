import createTheme from '@mui/material/styles/createTheme';


export const clotTheme = createTheme({
    typography: {
        fontFamily: [
            '"Prompt"', 
            'sans-serif'
        ].join(',')
    }
});
