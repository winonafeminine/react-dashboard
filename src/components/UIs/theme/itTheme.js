import createTheme from '@mui/material/styles/createTheme';


export const itTheme = createTheme({
    typography: {
        fontFamily: [
            '"Prompt"', 
            'sans-serif'
        ].join(',')
    }
});

